language: java
jdk:
  - oraclejdk8
sudo: false

services:
  - docker

script: mvn clean package

after_success:
  - docker login -u="$dockerhub_username" -p="$dockerhub_password"
  - docker build -t ricardobaumann/spring_blog_api:latest .
  - docker push ricardobaumann/spring_blog_api:latest